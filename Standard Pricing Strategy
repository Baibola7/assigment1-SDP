public class StandardPricingStrategy implements PricingStrategy {
    @Override
    public double calculatePrice(double basePrice) {
        // Implement the standard pricing strategy logic
        return basePrice;
    }
}
